# 常用egui组件

## 🟦 **1. 基础输入类**

| 组件            | 用法                                                                   | 功能        |
| ------------- | -------------------------------------------------------------------- | --------- |
| `Button`      | `ui.button("Click")`                                                 | 普通按钮      |
| `Label`       | `ui.label("Hello")`                                                  | 文本显示      |
| `TextEdit`    | `ui.text_edit_singleline(&mut s)` / `ui.text_edit_multiline(&mut s)` | 单行/多行文本输入 |
| `Checkbox`    | `ui.checkbox(&mut bool, "Enable")`                                   | 复选框       |
| `RadioButton` | `ui.radio(selected==1, "Option 1")`                                  | 单选按钮      |
| `ComboBox`    | `ComboBox::from_label("Select")`                                     | 下拉选择框     |
| `Slider`      | `ui.add(Slider::new(&mut value, 0.0..=1.0))`                         | 拖动条       |
| `DragValue`   | `ui.add(DragValue::new(&mut num))`                                   | 可拖拽修改数字   |

---

## 🟩 **2. 布局与容器**

| 组件                         | 功能      |
| -------------------------- | ------- |
| `CentralPanel`             | 主区域面板   |
| `SidePanel`                | 左/右侧边栏  |
| `TopPanel` / `BottomPanel` | 顶部/底部面板 |
| `Window`                   | 内部浮动窗口  |
| `Collapsing`               | 可折叠区域   |
| `ScrollArea`               | 滚动区域    |
| `Grid`                     | 表格布局    |
| `Separator`                | 分割线     |

---

## 🟨 **3. 显示与进度**

| 组件                  | 功能      |
| ------------------- | ------- |
| `Image` / `Texture` | 显示图像或纹理 |
| `Hyperlink`         | 超链接     |
| `ProgressBar`       | 进度条     |
| `Spinner`           | 加载动画    |

---

## 🟧 **4. 颜色与选择**

| 组件            | 功能    |
| ------------- | ----- |
| `ColorPicker` | 颜色选择器 |
| `ColorButton` | 颜色按钮  |

---

## 🟥 **5. 菜单类**

| 组件            | 功能   |
| ------------- | ---- |
| `MenuBar`     | 顶部菜单 |
| `ContextMenu` | 右键菜单 |

---

## 🟪 **6. 高级绘图 / 图表**

| 组件      | 功能                     |
| ------- | ---------------------- |
| `Plot`  | 绘图/曲线（egui 官方 plot 模块） |
| `Table` | 表格（egui_extras）        |

---

